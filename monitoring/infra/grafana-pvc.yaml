apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: prometheus-stack-grafana-longhorn
  namespace: monitoring
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 10Gi
