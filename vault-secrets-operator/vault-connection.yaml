# VaultConnection: in-cluster Vault address for VSO
apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultConnection
metadata:
  name: vault
  namespace: vault-secrets-operator
spec:
  address: "http://vault.vault.svc.cluster.local:8200"
