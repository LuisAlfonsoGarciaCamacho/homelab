apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: airflow-argo-dags-rwx
  namespace: airflow-argo
  annotations:
    argocd.argoproj.io/sync-wave: "-3"
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: longhorn-rwx
  resources:
    requests:
      storage: 1Gi
